from .models import *
import PIL
#Make this generate the maps for Commander (+world map)


def CommanderMap(commander):
    # All know hexes outside scout range through ObservedHex.
    # for old_hex in commander.observedHex_set.all():
    
    #Drawn all hexes in scout range using current hex information.
    # commander.location
    # commander.army_set.first().scout_distance #If a commander can have more than two armies, how do we pick the right one here?

    #EN DAN EEN KAART TEKENEN, WHATEVER
    
    

    return True


